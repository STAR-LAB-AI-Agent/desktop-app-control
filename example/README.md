# PowerShell 演示

两个脚本都会读取当前环境中的 `DEEPSEEK_API_KEY`，然后尝试激活 `desktop-ai`
环境并运行固定任务。先按照根目录 README 配好环境变量即可，不需要修改脚本内容。

执行通用搜索演示：

```powershell
.\example\run_general_demo.ps1
```

执行视频播放演示：

```powershell
.\example\run_video_demo.ps1
```
