# PowerShell 演示

每个脚本只包含三个步骤：设置 API 密钥、激活 `desktop-ai` 环境、运行固定演示任务。
请先打开脚本，把 `YOUR_API_KEY` 替换为自己的密钥。

执行通用搜索演示：

```powershell
.\example\run_general_demo.ps1
```

执行视频播放演示：

```powershell
.\example\run_video_demo.ps1
```
