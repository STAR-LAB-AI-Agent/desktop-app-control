# 脱敏运行记录

这里放了四个实际任务的精简记录：音乐播放、视频播放、打开应用，以及一次遇到登录和付费限制后停止的任务。

每个目录保留了 `task`、`review`、`result` 和操作记录，方便查看任务是怎么执行的。原始事件流和截图没有放进仓库。

公开前做了这些处理：

- 用户目录统一替换为 `<USER_HOME>`
- 删除用户名、账号、邮箱、密钥和令牌
- 隐去可能暴露浏览记录的网址
- 去掉截图、私人文件、聊天内容和重复文件

这些文件只是示例，不等同于本机保存的完整日志。
